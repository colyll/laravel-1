<?php

namespace App\Services\{{baseDirectory}};

use App\Repositories\{{baseDirectory}}\{{class}}Repository;
use App\Services\RedisTrait;
use App\Services\ServiceTrait;

class {{class}}Service
{

    use RedisTrait;
    use ServiceTrait;

    protected $repository;
    private $cache_head = '{{classLowerSeparators:}}:';

    public function __construct({{class}}Repository ${{classFirstLower}}Repository)
    {
        $this->connectRedis();
        $this->repository = ${{classFirstLower}}Repository;
    }

}
