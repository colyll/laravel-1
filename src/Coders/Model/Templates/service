<?php

namespace App\Services\{{baseDirectory}};

use App\Models\{{baseDirectory}}\{{class}};
use App\Services\Service;

class {{class}}Service extends Service
{
    protected string $cacheHead = '{{{classLowerSeparators:}}:';

    public function __construct({{class}} $model = null)
    {
        $this->connectRedis();
        $this->model = $model ?? {{class}}::query();
    }

    protected function configWheres($param) {
{{listBody}}
    }

    protected function configData($param) : array {
{{updateBody}}

        return $data;
    }

}
